{% macro events(editEvent, deleteEvent, bookmarkEvent) %}
    <div class="action-bar">
        {% import "button.njk" as editButton %}
        {{ editButton.settings("Edit", "edit", editEvent, "", true, "") }}
        {% import "button.njk" as deleteButton %}
        {{ deleteButton.settings("Delete", "delete", deleteEvent, "", true, "") }}
        {% import "button.njk" as bookmarkButton %}
        {{ bookmarkButton.settings("Bookmark", "bookmark", bookmarkEvent, "heart", true, "") }}
    </div>
{% endmacro %}